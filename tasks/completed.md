# Completed â€” CampusConnect

Finished work. Append, never rewrite. Latest on top.

| TASK | Title | Owner | Done | Summary |
|---|---|---|---|---|
| TASK-030 | UX polish | opencode/nishant | 2026-08-20 | Frontend UX/accessibility polish pass (frontend-only; TASK-029 deployment smoke test blocked on 027/028 creds, so polish applied to current app). 1) **Skeletons**: new `Skeleton` primitive (`components/ui/Skeleton.tsx` — `Skeleton`, `SkeletonRow`, `SkeletonCardGrid`, `SkeletonList` with pulse animation + `role="status"`/`aria-live` sr-only labels) replacing spinners in EventsView, ResourcesView, MyBookingsPage, AdminBookingsPage, AdminResourcesPage, AdminEventsPage. 2) **Accessibility**: global `:focus-visible` outline (Neo-Brutalist, in `index.css`), `prefers-reduced-motion` support disabling animations/transitions, skip-to-content link + `id="main-content"` in AppLayout, `aria-label` on nav elements. 3) **Responsive**: mobile nav menu in AppLayout (`md:hidden` hamburger toggling Home/My Bookings/Admin + Login/Logout; `aria-expanded`/`aria-controls`), since desktop nav was previously hidden on small screens. 4) **Modal UX**: all admin dialogs (reject booking, resource create/edit, event create/edit) now close on Escape and backdrop click (stopPropagation on panel). 5) **Success confirmation**: My Bookings cancel now shows a dismissible success/error banner (was silent). Tests: 8 new (Skeleton x5, MyBookings renders + cancel success, AdminBookings Escape closes dialog) — 55 total pass. Verified: lint/typecheck/build clean. |
| TASK-031 | Final documentation | opencode/manvar | 2026-08-20 | Finalized repo documentation: `README.md` (features, quick start, test accounts table, env vars, docs index, deployment + CI, team workflow); `docs/database-schema.md` refreshed PLANNED → IMPLEMENTED (migrations 0001–0009 applied live + merged, tables, RLS map, RPC table with availability/booking/event/update functions, business rules); new `docs/limitations.md` (scope/product/security/tech-debt limitations incl. pending advisor run); new `docs/roadmap.md` (6 phases: MVP shipped, production hardening, booking domain, notifications, admin insights, custom backend); `docs/current-state.md` final status (TASK-027 + 031 merged, remaining 028/029 with frontend teammate). Deployment URL left as instructions pending TASK-028 (Vercel). CI `lint-typecheck-build` will verify on PR. |
| TASK-027 | Supabase production configuration | opencode/manvar | 2026-08-20 |
| TASK-027 | Supabase production configuration | opencode/manvar | 2026-08-20 | `docs/production-config.md`: project info, migration matrix (0001–0009 all live + on main), security posture (RLS all tables, functions locked to authenticated, anon denied — backed by 28/28 live suite), auth config verified via `/auth/v1/settings` (email-only, confirm required, signup open, anon off), dashboard-only checklist (SMTP, site URL/redirects, rate limits, PITR, service-key rotation, seed gating), env-var strategy (`.env.example` + gitignored `.env`), tooling warning (supabase vs supabase-prod MCP targets). PENDING: security-advisor run on gmfhoq — `supabase` MCP server unavailable this session (advisors accidentally hit itpq project first; corrected in doc §7). Security posture otherwise established by 28/28 live suite. Deps TASK-024 (done). |
| TASK-023 | Admin resource/event management | opencode/nishant | 2026-08-20 | Admin CRUD for resources and events (frontend-only; RLS already allows admin writes). AdminResourcesPage (`/admin/resources`): grid of resource cards (status badge, location, quantity, capacity, booking-hour rules), search, create/edit modal form (name/category/location select/quantity/capacity/status/min-max/advance-notice/requires_approval/image/description with validation), Activate/Deactivate toggle, inline "+ Location" creator (fetchAllLocations/createLocation). AdminEventsPage (`/admin/events`): grid of event cards (status badge, date, location, registered_count/capacity, organizer via profiles join), search, create/edit modal (title/category/location/datetime-local start+end/capacity/status/image/description/requirements with validation incl. end>start), Publish (draft/cancelled→published) and Cancel (published→cancelled) quick actions. Shared `components/admin/AdminNav.tsx` (Bookings/Resources/Events links) on all three admin pages; routes wired in `router.tsx`. `lib/admin.ts` grew: fetchAllResources, createResource, updateResource, fetchAllEvents (locations+profiles joins), createEvent, updateEvent, fetchAllLocations, createLocation. Admin-only guard + redirect on each page. Tests: 8 new (4 per page) — 47 total pass. Verified: lint/typecheck/build clean. |
| TASK-026 | End-to-end student flow | opencode/nishant | 2026-08-20 | E2E integration test of the full student journey through the real app router: Login (ProtectedRoute → /login, sign-in form) → Home (EventsView) → Event detail → Register for event → Home → Resources tab → Resource detail → Book Now → BookingPage form (date/time/reason) → Submit → "Booking Request Submitted" + pending confirmation → View My Bookings → pending booking card in Active Bookings. `frontend/src/App.flow.test.tsx` renders `createMemoryRouter(routes, { initialEntries: ["/login"] })` with the real route tree; lib layer (AuthContext, events, resources, bookings) mocked per existing test conventions with mutable authState for the sign-in transition. `router.tsx` refactored to export `routes` (shared by app `createBrowserRouter` and test memory router). Verifies: registerForEvent('event-1','user-1') called, createBooking payload (resource/user/times/quantity/reason), "pending" confirmation, Pending badge inside the My Bookings card. 1 new test — 39 total pass. lint/typecheck/build clean. |
| TASK-019 (backend half) | Editable pending bookings RPC | opencode/manvar | 2026-08-19 | `0009_update_booking.sql`: `update_booking` RPC â€” SECURITY DEFINER, owner-only (`auth.uid()`), pending status only, partial updates via coalesce, re-validates availability (active resource, end>start, min/max duration, advance notice, overlap-vs-approved) under `pg_advisory_xact_lock`, stamps `updated_at`. `revoke` anon/public, `grant` authenticated. Applied live. Verified by extended security suite (6 new scenarios) + real client: 28/28 pass, zero residue. Unblocks frontend TASK-019 Edit. |
| TASK-022 | Admin approval/rejection | opencode/nishant | 2026-08-19 | Approve/Reject on AdminBookingsPage (`/admin/bookings`): Approve calls `supabase.rpc("approve_booking", { p_booking_id })`; Reject opens a modal with required reason (max 500 chars) then `supabase.rpc("reject_booking", { p_booking_id, p_rejection_reason })`. Buttons only on pending rows; success/error banner (dis), busy state disables actions, refetch after action. Admin-only enforced by RPCs (is_admin) + page redirect. `lib/admin.ts` approveBooking/rejectBooking. Added all merged backend RPC signatures (approve_booking, reject_booking, check_availability, create_booking, register_for_event, cancel_registration) to `lib/database.types.ts` Functions for typed `supabase.rpc`. Tests: 3 new (approve, reject with reason, reason required) â€” 38 total pass. Verified: lint/typecheck/build pass. |
| TASK-021 | Admin pending bookings | opencode/nishant | 2026-08-19 | AdminBookingsPage (`/admin/bookings`) per booking-requests-management design: bookings table (RLS `bookings_admin_all` reads all) with requestor profile join (name/email/department), resource (name/category/location), date/time, quantity, StatusBadge; filters (search across resource/requestor/email/reason, status, resource, date); expandable detail row (requestor card, resource card, quantity, requested-at, reason, special requirements, rejection-reason callout). `lib/admin.ts` `fetchAllBookings`. Admin nav link in AppLayout (visible only to role=admin); page redirects non-admins to `/`. Route wired in router. Approve/Reject actions intentionally NOT included (TASK-022). Tests: 3 new (renders rows, expands details, rejection reason) â€” 35 total pass. Verified: lint/typecheck/build pass. |
| TASK-024 | Database/RLS security tests | opencode/manvar | 2026-08-19 | `supabase/tests/security-tests.mjs` â€” real-client suite (plain fetch vs GoTrue+PostgREST, no deps) covering student isolation, admin access, unauthorized booking changes, duplicate registration, invalid booking times, overlapping bookings, inactive resources, draft/cancelled/past events, capacity, registered_count sync. Self-contained fixtures + cleanup in finally. Verified live: 22/22 passed, run twice, zero residue. All RLS policies correct, no DB defects found. |
| TASK-014 (backend half) | Event registration RPCs | opencode/manvar | 2026-08-19 | `0008_event_registration.sql`: `register_for_event`/`cancel_registration` (published + not-ended + capacity checks, unique-duplicate guard, `registered_count` sync). Fixed plpgsql bug: `select count(*)` clobbers `FOUND` â†’ INSERT never ran; now uses explicit `v_has_row` flag. Verified live via real client (register/duplicate/cancel/re-register/count). |
| TASK-022 | Admin booking workflow | opencode/manvar | 2026-08-19 | `0007_admin_booking_workflow.sql`: `approve_booking`/`reject_booking` (SECURITY DEFINER, `is_admin()` guard, re-validates availability/quantity at approve-time, sets approved_by/rejection fields) + `bookings_admin_all` RLS policy. Verified live: admin approve/reject, non-admin denied, overlap refused at approval. |
| TASK-017 | Secure booking creation | opencode/manvar | 2026-08-19 | `0006_secure_booking_creation.sql`: `create_booking` RPC (SECURITY DEFINER, user_id from auth, locks resource row, validates active/min-duration/advance-notice/end>start/overlap-vs-approved, races-safe quantity check). Verified live via real client. |
| TASK-016 | Availability validation | opencode/manvar | 2026-08-19 | `0005_booking_availability.sql`: `check_availability` RPC (overlap vs approved bookings, capacity, min-duration, advance-notice, resource-active checks). Verified live. |
| TASK-007 | Seed/development data | opencode/manvar | 2026-08-19 | `0004_seed_data.sql`: 4 auth users (admin + student1/2/3) with `auth.identities` + empty-string non-nullable-token fix for GoTrue v2.195, 5 locations, 6 events, resources, 8 seed bookings. All seed logins verified live (`Password123!`). |
| TASK-006 | RLS and authorization policies | opencode/manvar | 2026-08-18 | `0002_rls_policies.sql`: per-table RLS (profiles own-or-admin; events public-read published only; event_registrations own; resources public-read; bookings own + admin approve/reject) + `0003_harden_function_permissions.sql`: revoked function execute from anon/public, granted only authenticated; role-escalation trigger on profiles. Applied to live project + verified via MCP (own-row access, other-user denial, anon denied). |
| TASK-005 | Initial Supabase schema | opencode/manvar | 2026-08-18 | `0001_initial_schema.sql`: tables profiles, events, event_registrations, resources, bookings, locations (UUID PKs, FKs, timestamps, CHECK constraints, indexes, status enums/fields). Applied to live project `gmfhoqgskfgmppddtejh` (MCP `apply_migration`). |
| TASK-025 | Frontend component tests | opencode/nishant | 2026-08-18 | Set up Vitest 4.1.11 + React Testing Library 16.3.2 + jest-dom + user-event + jsdom in `frontend` (devDeps), `test` config in `vite.config.ts` (`vitest/config` defineConfig, jsdom, globals, `setupFiles: ./src/test/setup.ts` with `@testing-library/jest-dom/vitest`), `npm run test` scripts (root + frontend). Added 6 colocated test files (32 tests): StatusBadge (it.each statuses + unknown), EventCard (title/category/location/registered-count/link + fallbacks), ResourceCard (name/category/availability/location/link/quantity/capacity/maintenance), EmptyState, Button (render/disabled/onClick/type), BookingPage (resource summary, requestor info, quantity stepper "of 2 available"). CI `ci.yml` now runs Test step between typecheck and build. All 32 tests pass; lint/typecheck/build pass from root. Frontend-only. |
| TASK-020 | Booking detail/status UX | opencode/nishant | 2026-08-18 | BookingDetailPage (`/my-bookings/:bookingId`) per plan Â§11/Â§12: full booking summary (resource, category, pickup location, date, time, duration, quantity, reason, special requirements, requested-at) with booking reference; polished rejection panel ("Booking Rejected / Reason: â€¦" + rejected-at) and cancelled panel; per-status timeline (pendingâ†’approved/rejected/cancelled; approvedâ†’completed/cancelled) in sidebar; confirm-cancel with success/error banners; StatusBadge; loading/error/not-found states. `lib/bookings.ts` `fetchBookingById` (RLS user_id = auth.uid()). MyBookingsPage View Details / View Reason now link to the detail page (replaced inline expander). Frontend-only. Verified: lint/typecheck/build pass. |
| TASK-019 | Booking actions (frontend portion) | opencode/nishant | 2026-08-18 | Added actions to MyBookingsPage per plan Â§12: Pending/Approved â†’ Cancel (two-step confirm, busy state, refetch on success); Rejected â†’ inline "Booking Rejected. Reason:" callout + View Reason toggle; Cancelled/Completed â†’ View Details (expandable summary: reason, special requirements, requested-at). `lib/bookings.ts` `cancelBooking` â€” typed update to status='cancelled' + cancelled_at via RLS bookings_update_own_cancel (already live; no backend cancel RPC exists anywhere). BACKEND-BLOCKED: Edit (pending booking) â€” no RLS policy or RPC permits changing dates/quantity; Edit button omitted, needs teammate update_booking RPC (TASK-016/017 follow-up). Verified: lint/typecheck/build pass. |
| TASK-018 | My Bookings page | opencode/nishant | 2026-08-18 | MyBookingsPage (`/my-bookings`) per plan Â§12 + my-bookings design: fetches own bookings (RLS bookings_select user_id = auth.uid()) with resources join (name, category, image_url, locations); Active Bookings (pending/approved/rejected) + Booking History (cancelled/completed) sections with cards: resource thumb/initial, name (strikethrough when cancelled), StatusBadge, date range + start/end times, duration + quantity, category, pickup location; rejected cards show "Booking Rejected. Reason: â€¦" callout; loading/error/empty states. `lib/bookings.ts` `fetchMyBookings`. Actions (Edit/Cancel/View Details) deferred to TASK-019. Verified: lint/typecheck/build pass. |
| TASK-015 | Booking form | opencode/nishant | 2026-08-18 | BookingPage (`/resources/:resourceId/book`) per plan Â§10 + book-resource-projector design: Requestor card (name/email/department from profiles), WHEN DO YOU NEED IT (pickup date + start/end time slots + computed duration, quantity stepper only when quantity_available > 1), THE DETAILS (reason required, special requirements), resource preview sidebar (image, category badge, name, description, pickup location), live SUMMARY card (resource, date, time, duration, total items, reason) with SUBMIT BOOKING REQUEST + CANCEL. Submit inserts pending booking via typed client (`lib/bookings.ts` `createBooking`; RLS bookings_insert user_id = auth.uid()). Success state links to My Bookings; future-date + end>start + reason validation; not-bookable guard. Backend validation (overlap/min-max/advance notice) intentionally left to TASK-016/017. Verified: lint/typecheck/build pass. |
| TASK-014 | Resource detail | opencode/nishant | 2026-08-18 | ResourceDetailPage (`/resources/:resourceId`) per plan Â§9: hero banner (image/letter placeholder + category badge + StatusBadge availability), About section, Booking Rules (min/max hours, advance notice, approval required from schema fields), sidebar card (BOOK NOW â†’ `/resources/:resourceId/book` only when status active, else not-bookable notice; location, capacity, quantity available). `lib/resources.ts` `fetchResourceById` with locations join. Loading/error/not-found states. NOTE: teammate's "TASK-014 (backend half): Event registration" is a different task â€” plan TASK-014 Resource detail unclaimed at claim time. Verified: lint/typecheck/build pass. |
| TASK-013 | Event detail | opencode/nishant | 2026-08-18 | EventDetailPage (`/events/:eventId`) per event-details design: banner + title/category/status; About + Requirements sections; sidebar card (date range, time, location, seats with progress bar, organizer via profiles join); REGISTER action inserts into `event_registrations` (typed insert, RLS enforces user_id = auth.uid(); unique(event_id,user_id) prevents duplicates); registered state shown as disabled âœ“ Registered; success/error/loading/not-found states. Data via `lib/events.ts` `fetchEventById`/`fetchMyRegistration`/`registerForEvent`. Verified: lint/typecheck/build pass. |
| TASK-012 | Resources discovery | opencode/nishant | 2026-08-18 | Resources tab on Home now live: `lib/resources.ts` (typed `fetchResources` with locations join + search/category/availability/capacity/location filters, `fetchResourceFilterOptions`); `components/resources/ResourceCard.tsx` per neo-brutalist design (image/placeholder, StatusBadge availability, category, name, location, capacity/quantity); `ResourcesView.tsx` grid (1/2/3 col) with loading/error/empty states + filter selects; wired into HomePage resources tab. Only active resources shown as Available. Verified: lint/typecheck/build pass. |
| TASK-011 | Events discovery | opencode/nishant | 2026-08-18 | Events tab on Home now live: `lib/events.ts` (typed `fetchPublishedEvents` with locations join + search/category/date/location filters, `fetchEventFilterOptions`); `components/events/EventCard.tsx` per home design (image/placeholder, category badge, date, title, location/time/seats); `EventsView.tsx` grid (1/2/3 col) with loading/error/empty states + filter selects; wired into HomePage events tab (search from shell); added Material Symbols font + base style for icons. RLS verified: reads are authenticated-only (events not visible to anon â€” intended). Verified: lint/typecheck/build pass. |
| TASK-010 | Home shell and navigation | opencode/nishant | 2026-08-18 | HomePage per `design/screens/campusconnect-home.html`: welcome header with authenticated user's first name ("Welcome, {name} ðŸ‘‹"), EVENTS/RESOURCES tab switcher (existing `Tab`), responsive controls section (tabs + search input), and placeholder EmptyState content areas for the Events/Resources lists (filled by TASK-011/012). Navbar + Home routing already present. Verified: lint/typecheck/build pass. |
| TASK-008 | Student authentication | opencode/nishant | 2026-08-18 | `AuthProvider` (`lib/AuthContext.tsx`) wraps Supabase auth: session restore + `onAuthStateChange`, profile fetch (role/fullName), signIn/signUp/signInWithGoogle/signOut; `ProtectedRoute` now uses real session with loading state; `LoginPage` (login/signup modes, error handling, Google OAuth); `AppLayout` shows logged-in user name; removed auth stub. Verified: lint/typecheck/build pass. |
| TASK-003 | Supabase client setup | opencode/nishant | 2026-08-18 | `@supabase/supabase-js` added to frontend; typed `Database` types (`lib/database.types.ts`) matching teammate's `0001_initial_schema.sql` (profiles/locations/events/event_registrations/resources/bookings); singleton client `lib/supabase.ts` via `requireEnv()` (anon key only, no service-role); row/insert type aliases + status unions in `lib/types.ts`. Verified: lint/typecheck/build pass. |
| TASK-004 | Design system integration | opencode/nishant | 2026-08-18 | Campus Neo-Brutalist design system per `design/DESIGN.md` + `design-system.json`: full token map as Tailwind v4 `@theme` (colors/typography/spacing/radius/hard shadows, `.border-brutal`); UI primitives in `frontend/src/components/ui/`: Button (primary/secondary/ghost, squish + hard shadow), Card (interactive hover), Badge, StatusBadge (pending/approved/rejected/cancelled/completed/draft/published/active/inactive/maintenance), Input, Select, Tab, EmptyState, ErrorState; barrel export `ui/index.ts`. No redesign. Verified: lint/typecheck/build pass. |
| TASK-002 | Frontend architecture | opencode/nishant | 2026-08-18 | React+TS app structure (`pages/`, `components/`, `lib/`); react-router v6 routes (home/event/resource/booking/my-bookings/login/404); typed env vars (`env.d.ts`, `lib/env.ts`, `requireEnv`); reusable AppLayout (top bar + footer + Outlet); ErrorBoundary; LoadingSpinner; ProtectedRoute gate with auth stub (`lib/auth.ts`). No Supabase deps. Verified: lint/typecheck/build pass. |
| TASK-000 | Adopt Supabase plan + design export + onboarding | opencode/manvar | 2026-08-18 | Committed `docs/CampusConnect_Supabase_Team_Work_Plan.md` (canonical); DEC-005 Supabase pivot; exported Campus Neo-Brutalist design system to `design/` (DESIGN.md + tokens + 21 screen files); wrote `docs/team-onboarding.md`; rebuilt `tasks/backlog.md` to plan TASK-002..031 (old Express/Prisma backlog archived); updated architecture/current-state/schema/api-contracts/.env.example. DEC-005. |
| TASK-001 | Repo scaffolding | opencode/manvar | 2026-08-17 | npm workspaces monorepo; backend Express 5 + TS + health endpoint; frontend Vite 6 + React 18 + TS + Tailwind v4; ESLint flat config; CI (lint/typecheck/build); `npm install` + `npm run dev` verified booting both servers. DEC-004. |
